# Configurable watchOS In-Workout Screen — MERGED into the main spec

**Status**: SUPERSEDED. Configuration is no longer a future/separate feature — per user decision
it is folded directly into the main spec and MY-1282.

**See**: `specs/watch-in-workout-screen.md` §4 (four layer presets), §5 (layout invariants),
§7 (two-axis configuration architecture — preset × module toggles, storage, transport, iPhone
settings UI).

**Visual reference**: `specs/watch-in-workout-mockup.html` (4 presets + all states).

This file is kept only as a redirect so earlier links don't dangle. Do not implement from it.
